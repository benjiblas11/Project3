<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= movie.title %></title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="user-info">
        <h2>Your Status</h2>
        <form action="/update_status" method="POST">
            <input type="hidden" name="movie_rank" value="<%= movie.movie_rank %>">
            <label>
                <input type="checkbox" name="watched_status" <%= user.watched_status ? 'checked' : '' %>>
                Watched
            </label>
            <button type="submit" class="submit-btn">Update Status</button>
        </form>
    
        <h2>User Rating & Review</h2>
        <% if (user.user_rating && user.movie_review) { %>
            <p><strong>Your Rating:</strong> <%= user.user_rating %>/10</p>
            <p><strong>Your Review:</strong> <%= user.movie_review %></p>
            <a href="/edit_review/<%= movie.movie_rank %>" class="submit-btn">Edit</a>
        <% } else { %>
            <form action="/add_review" method="POST">
                <input type="hidden" name="movie_rank" value="<%= movie.movie_rank %>">
                <label for="rating">Rating:</label>
                <input type="number" id="rating" name="user_rating" min="1" max="10" class="input-field">
                <label for="review">Review:</label>
                <textarea id="review" name="movie_review" class="input-field"></textarea>
                <button type="submit" class="submit-btn">Submit</button>
            </form>
        <% } %>
    </div>    
</body>
</html>
